---
import Layout from '@layouts/Layout.astro';
import Newletter from '@components/Newletter.tsx';
---

<Layout
  title="Welcome to Astro."
  description="Home">
	<main class="fixed top-0 right-0 bottom-0 left-0 flex items-center justify-center">
    <div class="text-center mb-8">
      <h1 class="text-2xl font-bold mb-2">Subscribe to Our Newsletter</h1>
      <p>Stay updated with our latest news and announcements by subscribing to our newsletter.</p>
    </div>
    <form
      class="bg-white shadow-lg rounded-md p-5 md:p-10 flex flex-col w-11/12 max-w-lg group"
      novalidate
      name="newsletter"
      netlify
    >
      <label for="email" class="mb-5">
        <span>Email</span>
        <input
          type="email"
          name="email"
          id="email"
          class="w-full rounded border border-gray-300 bg-inherit p-3 shadow shadow-gray-100 mt-2 appearance-none outline-none text-neutral-800 invalid:[&:not(:placeholder-shown):not(:focus)]:border-red-500 peer"
          placeholder=" "
          required
          pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$"
        />
        <span class="mt-2 hidden text-sm text-red-500 peer-[&:not(:placeholder-shown):not(:focus):invalid]:block">
          Please enter a valid email address
        </span>
      </label>
      <button type="submit" class="mt-5 bg-blue-500 py-3 rounded-md text-white group-invalid:pointer-events-none group-invalid:opacity-50">
        Subscribe
      </button>
    </form>
	</main>
</Layout>
